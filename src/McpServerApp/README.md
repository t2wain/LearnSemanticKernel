# Example of MCP Server using STIO protocol

The best way to run this example is to run this node command in the command line console. Be sure to navigate to the project folder first before executing this node command.

```cmd
npx @modelcontextprotocol/inspector dotnet run
```

Be sure to select SDIO protocol before testing.