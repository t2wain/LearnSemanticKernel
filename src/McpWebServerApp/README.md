# Example of MCP Server using HTTP protocol

To test this command, run this application as a web server application. 

Next, run this node command in the command line console.

```cmd
npx @modelcontextprotocol/inspector
```
Besure to select HTTP protocol and enter the URL of the running web server application before testing.
